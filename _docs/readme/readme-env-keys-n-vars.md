# Keys an Variables

REMARK: This is a TEMPLATE, please update according to your needs...

## API Keys

| API KEY            | Description                                                                                                     |
| ------------------ | --------------------------------------------------------------------------------------------------------------- |
| OPENROUTER_API_KEY | API key for accessing OpenRouter, used to route requests to multiple LLM providers through a unified interface. |
| OPEN_AI_KEY        | API key for accessing OpenAI models (e.g., GPT models) for text generation, reasoning, and embeddings.          |
| PERPLEXITY_API_KEY | API key for accessing Perplexity AI, typically used for search, retrieval, and research-oriented queries.       |


## VARIABLES

| Variable | Description                                                                                                                         |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| HOST_IP  | Host address where the service will run (e.g., `0.0.0.0` for all interfaces or `127.0.0.1` for local development).                  |
| PORT     | Port number where the application will be exposed (e.g., `8000` for local development or a custom port in production environments). |

